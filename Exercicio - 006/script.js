var numero = Number (prompt(" - Informe um número: "));
/*6) Escreva um algoritmo para ler um valor (do teclado) e escrever (na tela) o seu suecessor. */
var resu = Number (numero + 1);

alert ("O sucessor do número informado é " + resu);